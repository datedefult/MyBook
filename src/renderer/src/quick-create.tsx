import React from 'react'
import ReactDOM from 'react-dom/client'
import QuickCreateApp from './QuickCreateApp'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <QuickCreateApp />
  </React.StrictMode>
)
